# ЗАССАН ХУВИЛБАР — 2026-09-03

Энэ хавтас нь `Cloud bot py 1` ботын **audit хийж зассан** хувилбар.
Эх хувилбар (`bot.py.bak` гэх мэт) нь хажуугийн `Cloud bot py 1` хавтаст хэвээр байгаа.

## Critical / High засварууд

| # | Асуудал | Засвар |
|---|---------|--------|
| 1 | `/fapi/v1/algoOrder` endpoint Binance дээр байхгүй → бүх SL/TP/Trailing амжилтгүй | Бүгд `POST /fapi/v1/order`-руу шилжсэн. `triggerPrice`→`stopPrice`, `activatePrice`→`activationPrice`, `algoType` хассан. Binance-ийн албан ёсны futures SDK-аар баталгаажуулсан. |
| 2 | Trailing идэвхтэй үед hard stop-loss байхгүй (liquidation хүртэл хамгаалалтгүй) | `rebuild_protection_orders` эхлээд emergency `STOP_MARKET` (`closePosition=true`) + `TAKE_PROFIT_MARKET` байрлуулна. Trailing нь нэмэлт, амжилтгүй бол ботыг зогсоохгүй. SL эсвэл TP амжилтгүй бол позиц огт нээгдэхгүй. |
| 3 | Хөшүүрэг / позицийн хэмжээ (5x, 6×9%) | `config.json` **хэвээр** — зөвхөн дутуу байсан per-trade SL нэмэгдсэн. |
| 4 | Дуусаагүй лаан дээр signal тооцох (repainting, backtest≠live) | `get_klines(drop_unclosed=True)` — одоо зөвхөн хаагдсан лаа. `analyze_coin` 260 лаа татаж, EMA-200-д хангалттай. |
| 5 | RSI: алдагдал=0 үед 50 гэж буруу гардаг | Цэвэр өсөлт → RSI 100, хавтгай цуврал → 50. |
| 6 | News mode бүх нээлттэй позицыг албадан хаадаг | `check_news_status` `safety_lock`-той холбоогүй болсон. News үед зөвхөн монитор, likвидаци хийхгүй. `last_news_check` TypeError мөн зассан. |
| 7 | Emergency close-ийн дараа өнчин order үлддэг | Хаасны дараа `cancel_all_symbol_orders`; `closePosition` order-ууд бас auto-cancel. |
| 8 | Rate-limit (429/418/-1003) хамгаалалт байхгүй → IP ban эрсдэл | `Retry-After`-тэй exponential backoff, public + signed хоёуланд. |

## Medium / Low засварууд

- **State persistence** — `session_state.json`-д drawdown peak / realized PnL хадгалж, restart дээр (24ц дотор) сэргээнэ.
- **Quantity формат** — `format_qty` / `format_price`: `1e-05` шинжлэх ухааны бичлэгээс сэргийлж, tickSize/stepSize-ийн оронтойгоор.
- **Order EXPIRED/0-fill** — `executedQty`/`avgPrice` шалгаж, дүүрээгүй бол хуурамч позиц үүсгэлгүй алгасна.
- **`unprotected_symbols`** — protection сэргэсэн үед цэвэрлэгдэнэ.
- **Funding threshold** — `0.01` → `0.0005` (өмнө нь бараг хэзээ ч ажилладаггүй байсан).
- **requirements.txt** — бүх хувилбар pin + `.python-version` (3.12) нэмэгдсэн.
- pytz `.replace(tzinfo=)` → `localize()`; `money()` сөрөг дүнгийн формат (`-$5.00`); correlation cache давхар key; Telegram 4096 тэмдэгтийн хязгаар; `telegram_format.py` давхар dict key.

## Шалгасан
- `python -m py_compile` — OK
- Логик тест (жинхэнэ pandas 3.0 / numpy 2.5 дээр): RSI, индикатор бүгд, signal generation, `get_klines` drop_unclosed — бүгд OK.

## Хараахан ШИЙДВЭР хэрэгтэй (код биш, тохиргоо)

1. **`BINANCE_BASE_URL`** — `.env` дэх `demo-fapi.binance.com` нь стандарт бус. Жинхэнэ Binance futures REST схемтэй тохирч байгааг баталгаажуул (засварууд стандарт `/fapi/` схемээр хийгдсэн).
2. **Railway volume** — volume mount хийхгүй бол `strategy_state.json` / `session_state.json` redeploy болгонд устна. Railway дээр volume нэмж, замыг зааж өгвөл persistence бүрэн ажиллана.

## Хойшлуулсан (аюулгүй, сонголтоор)
- `print()` → бүрэн logging framework
- Backtest-ийн O(n²) Supertrend дахин тооцоолол (default-оор унтраалттай)

## Deploy хийхийн өмнө
- `.env` файлаа шинэ хавтас руу хуулах (`env.example`-г үз)
- Хэрэв өмнө нь `strategy_state.json` байсан бол хуулж болно
